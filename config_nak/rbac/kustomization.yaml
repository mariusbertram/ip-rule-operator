resources:
# All RBAC will be applied under this service account in
# the deployment namespace. You may comment out this resource
# if your manager will use a service account that exists at
# runtime. Be sure to update RoleBinding and ClusterRoleBinding
# subjects if changing service account names.
- service_account.yaml
- role.yaml
- role_binding.yaml
- leader_election_role.yaml
- leader_election_role_binding.yaml

# For each CRD, "Admin", "Editor" and "Viewer" roles are scaffolded by
# default, aiding admins in cluster management. Those roles are
# not used by the ip-rule-operator itself. You can comment the following lines
# if you do not want those helpers be installed with your Project.
- agent_admin_role.yaml
- agent_editor_role.yaml
- agent_viewer_role.yaml
- iprule_admin_role.yaml
- iprule_editor_role.yaml
- iprule_viewer_role.yaml

# iprule-agent
- agent_cluster_role.yaml
- agent_cluster_role_binding.yaml
- agent_securitycontextconstraints.yaml
- agent_service_account.yaml
- agent-scc-clusterrole.yaml
- agent_scc_binding.yaml