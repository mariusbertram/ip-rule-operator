apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: iprule-agent-role
  labels:
    app.kubernetes.io/name: ip-rule-operator
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get", "list", "watch"]
- apiGroups:
    - api.operator.brtrm.dev
  resources:
    - ipruleconfigs
    - iprules
  verbs:
    - '*'
- apiGroups:
    - apps/v1
  resources:
    - daemonsets/finalizers
  verbs:
    - update
